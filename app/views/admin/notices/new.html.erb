<div class="active" id="">
  <p class="mt10">
    <a href="<%=admin_notices_path%>">通知管理</a> / 通知新建
  </p>
  <%= simple_form_for @notice, :url => { :controller => 'admin/notices', :action => 'create'} do |f| %>
      <%= render 'form', {:f => f }%>
  <% end %>
</div>