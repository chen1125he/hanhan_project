<div class="active" id="">
  <p class="mt10">
    <a href="<%=admin_plates_path%>">板块管理</a> / 板块新建
  </p>
  <%= simple_form_for @plate, :url => { :controller => 'admin/plates', :action => 'create'} do |f| %>
      <%= render 'form', {:f => f }%>
  <% end %>
</div>