<nav class="navbar navbar-default" role="navigation">
  <div class="navbar-header">
    <button class="navbar-toggle" type="button" data-target="#bs-example-navbar-collapse-1" data-toggle="collapse"> <span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span></button>
    <%= link_to home_path, :class => 'navbar-brand' do %>
        首页
    <% end %>
  </div>

  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav">
      <li>  <!-- class="active" -->
        <%= link_to discovers_path do %>
            发现
        <% end %>
      </li>
      <li>
        <%= link_to plates_path do %>
            主题
        <% end %>
      </li>

    </ul>
    <%= form_tag '/search', :class => 'navbar-form navbar-left', :role => 'search', :method => :post do%>
        <!--<div class="form-group">-->
        <!--<%= select_tag :plate_id, options_for_select(Plate.show_plates.pluck(:name, :id), params[:plate_id]), {:class => 'form-control', :include_blank => "请选择主题"}%>-->
        <!--</div>-->
        <div class="form-group">
          <%= text_field_tag :key_word,params[:key_word], {:placeholder => "帖子标题、简述、内容", :class => 'form-control'}%>
        </div>
        <button class="btn btn-default" type="submit">搜索</button>
    <% end %>
    <ul class="nav navbar-nav navbar-right">
      <li class="dropdown">
        <a class="dropdown-toggle" href="#" data-toggle="dropdown">我的<strong class="caret"></strong></a>
        <ul class="dropdown-menu">
          <li>
            <%= link_to "新建帖子", new_post_path%>
          </li>
          <li class="divider">
          </li>
          <% if current_user.present?%>
              <li>
                <%= link_to '我的帖子', my_posts_path%>
              </li>
              <li>
                <%= link_to '我的评论', my_comments_path%>
              </li>
              <li>
                <%= link_to '我的信息', :controller => "user_infos", :action => "edit"%>
              </li>
              <li>
                <%= link_to '我关注的', my_cares_url%>
              </li>
              <li>
                <%= link_to '关注我的', my_careds_path%>
              </li>
              <li>
                <%= link_to '密码修改', :controller => "users", :action => "edit"%>
              </li>
              <li class="divider">
              </li>
              <li>
                <%= link_to user_sessions_destroy_path, :method => 'delete', :data => {'confirm' => '要退出登录吗？'} do %>
                    退出登录
                <% end %>
              </li>
          <% else%>
              <li>
                <%= link_to new_user_session_path do %>
                    登录
                <% end %>
              </li>
              <li>
                <%= link_to new_register_path do %>
                    注册
                <% end %>
              </li>
          <% end %>

        </ul>
      </li>
    </ul>
  </div>

</nav>