<h1>
  推荐帖子：
</h1>
<% if @posts.present?%>
    <div class="row clearfix mt50">
      <%= render 'post_common/lines_per_6', :posts => @posts %>
    </div>
    <div class="text-right">
      <%= paginate @posts%>
    </div>
<% end %>